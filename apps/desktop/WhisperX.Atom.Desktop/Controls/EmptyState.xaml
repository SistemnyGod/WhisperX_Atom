<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WhisperX_Atom_Desktop.Controls.EmptyState"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" Spacing="7" Padding="20,26">
        <FontIcon x:Name="StateIcon" Glyph="&#xE8A5;" FontSize="22" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" />
        <TextBlock Text="{x:Bind Title, Mode=OneWay}" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
        <TextBlock Text="{x:Bind Description, Mode=OneWay}" Foreground="{StaticResource MutedTextBrush}" FontSize="{StaticResource CaptionTextSize}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="420" />
        <Button x:Name="ActionButton" Content="{x:Bind ActionText, Mode=OneWay}" Style="{StaticResource PrimaryButtonStyle}" Command="{x:Bind ActionCommand, Mode=OneWay}" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
