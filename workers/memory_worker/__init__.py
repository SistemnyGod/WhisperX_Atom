"""Owner-scoped Meeting Memory v2 indexing primitives.

The package is deliberately CPU/deterministic by default.  It produces
retrieval hints over transcript facts; canonical transcript segments remain
the only factual evidence source.
"""
