# Regression audio manifest

Audio files are intentionally kept outside Git. Place approved, anonymized recordings in this directory locally and update `manifest.json` with their SHA-256, duration, format and legacy baseline metrics.

The acceptance runner must never upload these recordings or include them in a commit.
